pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Add this line if you find any plugins/dependencies not available in the above repositories.
        // gradlePluginPortal()
    }
}

rootProject.name = "MediaApp"
include ':app'
